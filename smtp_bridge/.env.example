# Zeabur API Key（二选一）:
# 方式1: 在这里固定写死，SMTP 不需要密码认证
# ZEABUR_API_KEY=zs_your_api_key_here

# 方式2: 留空，由 SMTP 客户端在密码字段传入 API Key（更灵活）
ZEABUR_API_KEY=

# SMTP 监听设置
SMTP_HOST=0.0.0.0
SMTP_PORT=2525

# Zeabur API 地址（一般不用改）
ZEABUR_API_URL=https://api.zeabur.com/api/v1/zsend
